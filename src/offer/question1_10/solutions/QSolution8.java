package offer.question1_10.solutions;

public class QSolution8 {

    /*
     * 二叉树的下一个结点
     *
     * 1.如果给定的结点 node 有右子树，那么它的下一个节点就是它的右子树中最左下角的结点。
     * 2.如果给定的结点 node 没有右子树：
     *      1).如果 node 是它父结点的左结点，那么下一个节点就是 node 的父结点
     *      2).如果 node 是它父结点的右节点，那么可以沿着指向父结点的指针一直向上遍历，
     *      直到找到一个是它父结点的左子节点的结点
     */

}
