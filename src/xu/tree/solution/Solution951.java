package xu.tree.solution;

public class Solution951 {
    /*
     * 翻转等价二叉树
     *
     * 判断两个二叉树是否相等可以使用如下方法进行：
     *
     * 1.root1 = root2 = null，那么两个二叉树相等，返回 true
     * 2.root1, roo2 其中只有一个等于 null 的话，返回 false
     * 3.root1 和 root2 的 val 如果不相等的话，直接返回 false
     * 4.接下来分为两种情况来进行处理：
     *      i.不进行翻转，判断两个二叉树是否相等，赋值给变量 flip
     *      ii.进行翻转，判断两个二叉树是否相等，赋值给变量 noflip
     *      iii.最后返回 flip || noflip，也就是只要上面两种情况中的任意一种判断两个二叉树相等，那么就返回 true
     */
}
