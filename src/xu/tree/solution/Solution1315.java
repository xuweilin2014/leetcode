package xu.tree.solution;

public class Solution1315 {
    /*
     * 祖父节点值为偶数的节点和
     *
     * 判断祖父结点为偶数，可以反过来判断，当一个结点的值为偶数时，可以判断其孙子结点是否为 null，如果不为 null，
     * 则将其值加到总和 sum 中。最后遍历完整个链表之后直接返回 sum。
     */
}
